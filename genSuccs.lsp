
(defun gen_succs (state) *dimension*
    (let blankPos  ;local variable of blank tile index
    (setf blankPos (position 0 state :test #'equal))  ;get position of blank tile
    (cond
        
     )

     ) 

)     
    
    